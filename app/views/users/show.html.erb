<!DOCTYPE html>
<html>
  <head>
    <title>Profile Page</title>
  </head>

  <body class="bg-light">
    <div class="container mt-5 p-4 bg-white rounded shadow">
      <h1 class="text-center text-primary">Welcome, <%= @user.firstname %>!</h1>

      <p class="lead"><strong>Username:</strong> <%= @user.username %></p>
      <p class="lead"><strong>Email:</strong> <%= @user.email %></p>
      <p class="lead"><strong>Full Name:</strong> <%= @user.firstname %> <%= @user.lastname %></p>

      <div class="text-center mt-4">
        <%= link_to 'Edit Profile', edit_user_path(@user), class: 'btn btn-primary btn-lg' %>
        <%= link_to 'Send Credentials', new_user_user_cred_path(@user), class: 'btn btn-primary btn-lg' %>
      </div>
      <% if !current_user.nil? && (current_user.admin?)%>
      <%= link_to "Approve Credentials", user_creds_path, class: "btn btn-info" %>   
      <% end %>
       </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.4.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </body>
</html>
